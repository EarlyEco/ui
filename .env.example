# Local backend (npm run dev proxies /api here)
BE_BASE_URL=http://127.0.0.1:8000

# Production / cloud UI builds: set to your deployed API origin (no trailing slash), e.g.
# BE_BASE_URL=https://be-xi-sandy.vercel.app
